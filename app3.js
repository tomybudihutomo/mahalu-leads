function renderAds(){const spend=spends.reduce((a,b)=>a+Number(b.spend||0),0),paid=leads.filter(x=>x.entry_type==='ads').length,deals=leads.filter(x=>x.entry_type==='ads'&&x.status==='deal').length;$('adKpis').innerHTML=[['Total Spend',fmtRp(spend),'semua periode'],['Paid Leads',paid,'leads dari iklan'],['CPL',fmtRp(paid?spend/paid:0),'biaya / lead'],['Paid Deal',deals,paid?(deals/paid*100).toFixed(1)+'% conversion':'0%']].map(x=>`<div class="kpi"><span>${x[0]}</span><strong>${x[1]}</strong><small>${x[2]}</small></div>`).join('');$('adTable').innerHTML=spends.length?`<div class="table-wrap"><table><thead><tr><th>Tanggal</th><th>Akun</th><th>Campaign</th><th>Spend</th><th>Catatan</th></tr></thead><tbody>${spends.map(s=>`<tr><td>${dOnly(s.spend_date)}</td><td>${esc(chName[s.account])}</td><td><b>${esc(s.campaign_name)}</b></td><td>${fmtRp(s.spend)}</td><td>${esc(s.notes||'-')}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">Belum ada ad spend.</div>'}
function renderCampaigns(){let m={};spends.forEach(s=>{let k=`${s.account}|${s.campaign_name}`;m[k]??={account:s.account,campaign:s.campaign_name,spend:0,leads:0,deals:0,rev:0};m[k].spend+=Number(s.spend||0)});leads.filter(l=>l.entry_type==='ads').forEach(l=>{let k=`${l.channel}|${l.campaign_name||'(Tanpa Campaign)'}`;m[k]??={account:l.channel,campaign:l.campaign_name||'(Tanpa Campaign)',spend:0,leads:0,deals:0,rev:0};m[k].leads++;if(l.status==='deal'){m[k].deals++;m[k].rev+=Number(l.deal_value||0)}});let a=Object.values(m);$('campaignReport').innerHTML=a.length?`<div class="table-wrap"><table><thead><tr><th>Campaign</th><th>Akun</th><th>Spend</th><th>Leads</th><th>Deal</th><th>Conv.</th><th>CPL</th><th>CPA</th><th>ROAS</th></tr></thead><tbody>${a.map(x=>`<tr><td><b>${esc(x.campaign)}</b></td><td>${esc(chName[x.account]||x.account)}</td><td>${fmtRp(x.spend)}</td><td>${x.leads}</td><td>${x.deals}</td><td>${x.leads?(x.deals/x.leads*100).toFixed(1):0}%</td><td>${fmtRp(x.leads?x.spend/x.leads:0)}</td><td>${fmtRp(x.deals?x.spend/x.deals:0)}</td><td>${x.spend?(x.rev/x.spend).toFixed(2):'0.00'}x</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">Belum ada data campaign.</div>'}
async function saveSpend(e){e.preventDefault();let payload={spend_date:$('spendDate').value,account:$('spendAccount').value,campaign_name:$('spendCampaign').value.trim(),spend:Number($('spendValue').value),notes:$('spendNotes').value||null,created_by:session.user.id};let{error}=await db.from('leads_ad_spend').upsert(payload,{onConflict:'spend_date,account,campaign_name'});if(error){alert(error.message);return}$('spendModal').classList.add('hidden');$('spendForm').reset();$('spendDate').value=new Date().toISOString().slice(0,10);await loadData()}
function switchSection(id){document.querySelectorAll('.nav button').forEach(x=>x.classList.toggle('active',x.dataset.section===id));document.querySelectorAll('.section').forEach(x=>x.classList.toggle('active',x.id===id))}
$('loginBtn').onclick=async()=>{const b=$('loginBtn');b.disabled=true;b.textContent='Masuk...';$('loginError').classList.add('hidden');let{error}=await db.auth.signInWithPassword({email:$('email').value.trim(),password:$('password').value});b.disabled=false;b.textContent='Masuk';$('loginError').classList.toggle('hidden',!error);$('loginError').textContent=error?.message||''};
$('logoutBtn').onclick=()=>db.auth.signOut();document.querySelectorAll('.nav button').forEach(b=>b.onclick=()=>switchSection(b.dataset.section));document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>$(b.dataset.close).classList.add('hidden'));$('newLeadBtn').onclick=$('newLeadBtn2').onclick=startNew;$('newSpendBtn').onclick=()=>{$('spendModal').classList.remove('hidden')};$('spendForm').onsubmit=saveSpend;$('refreshBtn').onclick=renderDashboard;$('dashFrom').onchange=renderDashboard;$('dashTo').onchange=renderDashboard;$('searchLead').oninput=renderLeadLists;$('filterStatus').onchange=renderLeadLists;$('filterSource').onchange=renderLeadLists;boot();
